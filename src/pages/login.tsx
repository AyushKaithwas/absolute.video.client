import Layout from "@/components/layout";
import Login from "@/sections/auth/login";
import React from "react";

function page() {
  return (
    <Layout>
      <Login />
    </Layout>
  );
}

export default page;
